#include <stdio.h>

/**
*main - printing lowercase alphabet
*Return: Always 0 (Success)
*/

int main(void)
{
char alphabet = 'a';

while (alphabet <= 'z')
{
putchar(alpha);
alpha++;
}
putchar('\n');

return (0);
}
